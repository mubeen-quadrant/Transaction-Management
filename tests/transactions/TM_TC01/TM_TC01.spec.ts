import { test } from "@playwright/test";

/** TM_TC01 — replace title/steps with your test plan. */
test.describe("TM_TC01", () => {
  test.skip("TM_TC01: implement (e.g. login / entry flow)", async () => {
    // await page.goto("/login");
  });
});
